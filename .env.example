# OANDA API Configuration
OANDA_TOKEN=changeme
OANDA_ACCOUNT_ID=changeme
OANDA_ENV=practice  # Options: practice, live

# Optional: Error notification webhook (e.g., Slack)
ERROR_WEBHOOK_URL=https://your-webhook-here

# Risk management (optional, defaults to 2%)
RISK_FRAC=0.02

# Optional: Set to 0 to disable health server
ENABLE_HEALTH=1

# CI flag (set automatically in GitHub Actions)
# CI=true